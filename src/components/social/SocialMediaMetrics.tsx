import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const SocialMediaMetrics: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Social Media Campaign Metrics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Metrics dashboard will be implemented here</p>
      </CardContent>
    </Card>
  );
};